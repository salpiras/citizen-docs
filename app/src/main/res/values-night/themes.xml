<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Dark parent so the pre-Compose window decor matches too, not just the background. -->
    <style name="Theme.CitizenDocs" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
